from ucrc import crc8_sae_j1850
from ucrc import crc8_sae_j1850_zero
from ucrc import crc8_8h2f
from ucrc import crc8_cdma2000
from ucrc import crc8_darc
from ucrc import crc8_dvb_s2
from ucrc import crc8_ebu
from ucrc import crc8_icode
from ucrc import crc8_itu
from ucrc import crc8_maxim
from ucrc import crc8_rohc
from ucrc import crc8_wcdma

def test_crc8_sae_j1850():
    assert crc8_sae_j1850(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0xC6


def test_crc8_sae_j1850_zero():
    assert crc8_sae_j1850_zero(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x30


def test_crc8_8h2f():
    assert crc8_8h2f(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0xB9


def test_crc8_cdma2000():
    assert crc8_cdma2000(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0xFE


def test_crc8_darc():
    assert crc8_darc(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x65


def test_crc8_dvb_s2():
    assert crc8_dvb_s2(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x31


def test_crc8_ebu():
    assert crc8_ebu(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x4B


def test_crc8_icode():
    assert crc8_icode(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0xB5


def test_crc8_itu():
    assert crc8_itu(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x85


def test_crc8_maxim():
    assert crc8_maxim(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x34


def test_crc8_rohc():
    assert crc8_rohc(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x8


def test_crc8_wcdma():
    assert crc8_wcdma(bytes([0xFF, 0xFE, 0xFA, 0xF0, 0xFF, 0x00])) == 0x68